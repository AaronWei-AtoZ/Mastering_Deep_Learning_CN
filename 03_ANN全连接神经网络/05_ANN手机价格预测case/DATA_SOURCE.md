# 数据准备

本案例使用 Kaggle 的 [Mobile Price Classification](https://www.kaggle.com/datasets/iabhishekofficial/mobile-price-classification) 数据结构。

原数据页面目前将许可证标记为 `Unknown`，因此仓库不重新分发 CSV。请自行下载包含 `price_range` 标签的训练数据，将其改名为 `手机价格预测.csv` 并放在当前目录。

